{
  "name": "project-planner-functions-push",
  "description": "Remote push for org notifications (no secrets; deploy even if email functions are blocked)",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
